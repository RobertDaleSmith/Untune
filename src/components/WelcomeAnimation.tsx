export function WelcomeAnimation() {
  return (
    <video
      src="/icon.mp4"
      autoPlay
      loop
      muted
      playsInline
      className="max-w-[280px] max-h-[280px] w-full h-full object-contain"
    />
  );
}
